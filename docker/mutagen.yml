sync:
  frontend:
    alpha: ../
    beta: docker://dataflow-frontend-1/app
    ignore:
      vcs: true
      paths:
        - node_modules
        - .next
        - .git
        - .env*

  ws-gateway:
    alpha: ../
    beta: docker://dataflow-ws-gateway-1/app
    ignore:
      vcs: true
      paths:
        - node_modules
        - dist
        - .git
        - .env*
  
  runner:
    alpha: ../
    beta: docker://dataflow-runner-1/app
    ignore:
      vcs: true
      paths:
        - node_modules
        - dist
        - .git
        - .env*
